# context/

エージェントが参照する個人コンテキストを定義するディレクトリ。

このディレクトリのファイルは個人の情報（パス、リポジトリ名、役割等）を含むため `.gitignore` で除外している。

## 必要なファイル

### shared.md
ユーザーのコンテキスト（役割、課題感、思考原則など）と、すべてのエージェントが共有する基本原則を定義する。

### sources.md
エージェントが参照する情報ソースのパスマッピングとアクセスパターンを定義する。どのシステムの何をどの頻度で読むかのガイドライン。
